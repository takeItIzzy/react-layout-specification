import * as React from 'react';
import { IComponentBaseProps } from '@/components/index.types';

const Content: React.FC<IComponentBaseProps> = ({ typeKey, children }) => {
  // todo
  return <div>{children}</div>;
};

export default Content;
